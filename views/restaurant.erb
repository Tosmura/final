<div class="row">
    <div class="col-sm-9">
      <h1><%= @restaurant[:title] %></h1>
        <p><%= @restaurant[:description] %></p>
        <% for rsvp in @rsvps %>
            <% user = @users_table.where(:id => rsvp[:user_id]).to_a[0] %>
            <div class="media">
                <% if rsvp[:going] %>
                    <i class="fas fa-thumbs-up fa-3x mr-3"></i>
                <% else %>
                    <i class="fas fa-thumbs-down fa-3x mr-3"></i>
                <% end %>
                <div class="media-body">
                    <h5 class="mt-0"><%= user[:name] %></h5>
                    <%= rsvp[:comments] %>
                </div>
            </div>
        <% end %>
        <p class="mt-5">
            <a href="/restaurants/<%= @restaurant[:id] %>/rsvps/new" class="btn btn-primary">Review now!</a>
        </p>
    </div>
    <div class="col-sm-3">

        <h3>Where?</h3>
        <p><%= @restaurant[:location] %></p>
        <iframe
            width="450"
            height="300"
            frameborder="0" 
            style="border:0"
            src="https://www.google.com/maps/embed/v1/place?key=AIzaSyCtovsQvkIUWlNqtYwXY87gEd4ZSmJEhMw&q=<%= @restaurant[:location]%>&zoom=15" allowfullscreen>
        </iframe>
        <h3>How many guests said great so far?</h3>
        <p><%= @count %></p>
    </div>
</div>

  